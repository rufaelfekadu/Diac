# Lightning-based configuration for LSTM model
# Extends the base LSTM configuration with Lightning-specific settings

MODEL:
  TYPE: 'Transformer'
  MAXLEN: 272
  VOCAB_SIZE: 77
  ASR_VOCAB_SIZE: 91
  D_MODEL: 256
  NUM_HEADS: 4
  DFF: 128
  NUM_BLOCKS: 2
  DROPOUT_RATE: 0.2
  OUTPUT_SIZE: 19
  USE_ASR: True
  PRETRAINED_PATH: null
  LOAD_TEXT_BRANCH_ONLY: False
  WITH_CONN: False

TRAIN:
  DEVICE: 'cuda'
  BATCH_SIZE: 128
  NUM_EPOCHS: 120
  LEARNING_RATE: 0.001
  SAVE_FREQ: 30
  EVAL_FREQ: 1
  SAVE_DIR: 'checkpoints/transformer-sine-text-only/tashkeela'
  EARLY_STOPPING_PATIENCE: 20
  ACCUMULATE_GRAD_BATCHES: 1
  VAL_CHECK_INTERVAL: 1.0

INFERENCE:
  MAX_LENGTH: 600
  WINDOW_SIZE: 50
  BUFFER_SIZE: 25
  DEVICE: 'cuda'
  BATCH_SIZE: 16
  MODEL_PATH: 'outputs/results/transformer-text+asr/tashkeela+clartts/tensorboard/version_0/checkpoints/best_model.ckpt'
  ASR_MODEL_NAME: 'sashat/whisper-medium-ClassicalAr'
  USE_ASR: True
  FORCED_IDS: null
  OUTPUT_PATH: 'results/temp.txt'

DATA:
  TRAIN_PATH: 'data/tashkeela/train.txt'
  VAL_PATH: null
  TEST_PATH: 'data/clartts/test.txt'
  MAX_LENGTH: 270

CONSTANTS_PATH: 'constants/'